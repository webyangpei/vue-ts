import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
      app: {
          device: 'pc',
          sidebar: 'pc'
          // permission_routers:
      }
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
